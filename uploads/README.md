# Uploads Folder

This folder stores uploaded files:
- Question files (if lecturers upload files for file-based questions)
- Student submissions (file answers)

Notes:
- Do not remove `uploads/.htaccess`. It blocks script execution and directory listing.
- In production, you may want to exclude this folder from Git and back it up separately.
